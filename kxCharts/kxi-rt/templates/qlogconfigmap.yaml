apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kxi-rt.fullname" . }}-log
  labels:
    {{- include "kxi-rt.labels" . | nindent 4 }}
data:
  qlog.json: |-
    {
      "endpoints": [
        "fd://stdout"
      ],
      "formatMode": "json",
      "routings": {
        "DEFAULT": "INFO"
      }
    }