apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kxi-sp.fullname" . }}-log
  labels:
    {{- include "kxi-sp.labels" . | nindent 4 }}
data:
  qlog.json: |-
    {
      "endpoints": [
        "fd://stdout"
      ],
      "formatMode": "json",
      "routings": {
        "DEFAULT": "INFO"
      }
    }